docker run --name lite-techne-catalogDB -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=Admin1234@" -p 1433:1433 --hostname db-lite-techne-catalog -d mcr.microsoft.com/mssql/server:2025-latest   

este comando que ves aqui lo que busca es crear el servidor para ahi crear la bd dentro 
de ella por ejemplo catalogDB despues de ejecutar el comando no se creara tendras que 
activar la imagen entrar al servidor y crear la bd con sql puro

PD: el hostname si genera el nombre del servidor que vamos a visualizar en dbeaver es 
para que no salga master en la cabecera de la conexion