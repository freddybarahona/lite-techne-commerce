export class InitializeConstants{
  static DB_CONNECTION_ESTABLISHED="Conexion BD establecida"
  static infoBackActive(port: number){
    return `Auth service corriendo en http://localhost:${port}`
  }
}